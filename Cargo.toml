[package]
name = "ao3-scraper"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.12", features = ["blocking", "cookies"] }
scraper = "0.22"
clap = { version = "4", features = ["derive"] }
regex = "1"
sanitize-filename = "0.6"
